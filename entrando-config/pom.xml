<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>co.com.entrando</groupId>
		<artifactId>entrando-backend</artifactId>
		<version>0.0.1-SNAPSHOT</version>
		<relativePath>../pom.xml</relativePath>
	</parent>
	<properties>
		<spring-boot.build-image.builder>paketobuildpacks/builder:base</spring-boot.build-image.builder>
		<spring-boot.build-image.imageName>46.202.92.9:5000/entrando-config:${project.version}</spring-boot.build-image.imageName>
	</properties>
	<groupId>co.com.entrando.config</groupId>
	<artifactId>entrando-config</artifactId>
	<version>0.0.4-SNAPSHOT</version>
	<name>entrando-config</name>
	<description>entrando-config</description>
	<dependencies>
		<dependency>
			<groupId>org.springframework.cloud</groupId>
			<artifactId>spring-cloud-config-server</artifactId>
		</dependency>
	</dependencies>
	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<configuration>
					<image>
						<builder>${spring-boot.build-image.builder}</builder>
						<name>${spring-boot.build-image.imageName}</name>
						<env>
							<BP_JVM_VERSION>${java.version}</BP_JVM_VERSION>
							<BP_JAVA_APP_SERVER>tomcat</BP_JAVA_APP_SERVER>
						</env>
					</image>
				</configuration>
			</plugin>
		</plugins>
	</build>
</project>
